# 애플리케이션 정보

## 이름
VocaBokum

## github repo 정보
https://github.com/superwiss/VocaBokum


# 애플리케이션 사용 시나리오

- VocaBokum은 영어 단어를 외우는데 도움을 주는 웹 애플리케이션이다.
- 사용자는 하루에 N개(사용자가 선택 가능)의 단어를 암기한다. 이후 VocaBokum 을 이용해서 영어 단어를 제대로 암기했는지 확인한다.
- 시험은 아래와 같은 3가지 유형으로 출제가 된다.
  - 유형 1) VocaBokum이 발음을 들려준다. 그러면 사용자는 그 발음을 듣고 한글 뜻을 입력한다.
  - 유형 2) VocaBokum이 화면에 영어 단어를 띄워준다. 그러면 사용자는 한글 뜻을 입력한다.
  - 유형 3) VocaBokum이 화면에 한글 뜻을 입력한다. 그러면 사용자는 영어 단어를 입력한다.
- 사용자가 오늘 2개의 단어인 N1과 N2를 익혔다면, 시험은 단어 수 * 유형 3종류 = 6개의 문제를 출제한다. 단어와 유형은 랜덤으로 선택된다. 예를 들어
  - 문제 1) VocaBokum이 N2에 대해서 유형 3에 대한 문제를 낸다.
  - 문제 2) VocaBokum이 N1에 대해서 유형 1에 대한 문제를 낸다.
  - 문제 3) VocaBokum이 N2에 대해서 유형 1에 대한 문제를 낸다.
  - 문제 4) VocaBokum이 N2에 대해서 유형 2에 대한 문제를 낸다.
  - 문제 5) VocaBokum이 N1에 대해서 유형 3에 대한 문제를 낸다.
  - 문제 6) VocaBokum이 N1에 대해서 유형 2에 대한 문제를 낸다.
- 복습 기능이 있다. 즉 사용자가 지금까지 익혔던 단어들을 대상으로 K개(사용자가 선택 가능)를 입력하면, 과거에 학습했던 단어 중 랜덤하게 1개의 유형을 선택해서 출제한다.
  이 때, 100% 랜덤은 아니고, 많이 틀렸던 단어가 복습 기능을 통해서 다시 출제되는 확률이 더 높다.
- 유형 1과 2의 경우, 사용자는 한글 뜻을 입력해야 하는데, 단어에 뜻이 여러 개 있을 경우, 1개만 입력해도 정답 처리를 한다.
  - 답변 검증 시 괄호, 쉼표, 마침표 등 특수문자는 무시한다.
  - 공백(white space)도 무시한다. 예: "땅에 묻다"와 "땅에묻다"는 동일하게 처리된다.
  - 한국어 동사의 능동태/피동태/사동태 구분을 하지 않는다. 예: "고정하다", "고정시키다", "고정되다"는 모두 정답으로 인정된다.
- 사용자는 오늘 새로 익힐 단어 또는 숙어를 입력할 때 영어 단어/숙어와 한글 뜻을 함께 입력한다. 영어 발음은 인터넷 OpenAPI를 통해서 자동으로 가져온다. 한글 뜻은 여러 개 입력할 수 있으며, 쉼표(,)로 구분한다.
  - 단일 단어 예: "hello" → "안녕, 인사"
  - 숙어 예: "at first" → "처음에는"
  - API에서 발음을 찾지 못하는 경우(주로 숙어) 기본 발음 형식을 사용한다.
- 사용자가 오늘 입력한 단어는 오늘 시험 대상이 된다. 내일이 되면 이 단어는 복습 기능 대상이 된다.
- 사용자가 학습 대상으로 입력한 단어들을 import / export 할 수 있는 기능이 제공된다.
- 사용자는 날짜별로 합습 대상을 입력할 수 있다. 이 때, 사용자는 오늘 새로 입력한 영어 단어 뿐만 아니라, 과거 날짜에 학습했던 영어 단어를 대상으로 시험을 볼 수도 있다.
- 관리자 페이지를 제공한다. 관리자 페이지에서는 날짜 별 새로 등록된 영어 단어들 + 영어 단어 별 시험 합격 비율을 보여준다.

# 아키텍쳐
- B/E 가 없는 F/E 전용 애플리케이션이다.
- 사용자가 입력하거나 import 한 학습 대상 단어는 로컬 스토리지에 저장이 된다.
- react + shadcn 조합을 사용한다.

