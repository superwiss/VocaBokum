# 애플리케이션 정보

## 이름
VocaBokum

## github repo 정보
https://github.com/superwiss/VocaBokum


# 애플리케이션 사용 시나리오

- VocaBokum은 영어 단어를 외우는데 도움을 주는 웹 애플리케이션이다.
- 사용자는 영어 단어를 학습하고, VocaBokum을 이용해서 영어 단어를 제대로 암기했는지 시험을 통해 확인한다.
- 시험은 아래와 같은 4가지 유형으로 출제가 된다.
  - 유형 1) VocaBokum이 발음을 들려준다. 그러면 사용자는 그 발음을 듣고 한글 뜻을 입력한다.
    - 문제 화면 진입 시 자동으로 발음이 재생된다.
  - 유형 2) VocaBokum이 발음을 들려준다. 그러면 사용자는 그 발음을 듣고 영어 단어를 입력한다.
    - 문제 화면 진입 시 자동으로 발음이 재생된다.
  - 유형 3) VocaBokum이 화면에 영어 단어를 띄워준다. 그러면 사용자는 한글 뜻을 입력한다.
  - 유형 4) VocaBokum이 화면에 한글 뜻을 입력한다. 그러면 사용자는 영어 단어를 입력한다.
- 사용자는 시험을 시작하기 전에 다음 항목들을 선택할 수 있다:
  - 단어장 선택: 학습할 단어장을 선택한다 (멀티 셀렉트 가능).
    - 사용자는 먼저 단어장을 생성하고, 단어장에 단어를 등록한다.
    - 시험 시작 전에 원하는 단어장을 여러 개 선택할 수 있다.
    - 기본값은 첫 번째 단어장이 선택된 상태이다.
  - 출제할 유형: 예를 들어, 유형 1과 3만 선택하면 해당 유형의 문제만 출제된다. 기본적으로 4가지 유형 모두 출제된다.
  - 채점 방식: 즉시 채점 모드 또는 일괄 채점 모드를 선택할 수 있다.
    - 즉시 채점 모드: 문제를 풀 때마다 정답/오답 여부를 바로 알려주고, 실시간으로 현재 점수를 표시한다. 피드백 화면에서 엔터키를 눌러 다음 문제로 진행할 수 있다.
    - 일괄 채점 모드: 모든 문제를 푼 후 한꺼번에 채점 결과(문제별 정답/오답, 총점)를 표시한다.
    - 기본값은 즉시 채점 모드이다.
  - 문제 수: 선택한 단어장 내의 단어 중 몇 개를 시험 볼지 선택한다. 기본값은 선택한 단어장의 모든 단어이다.
- 선택한 단어장에 2개의 단어 N1과 N2가 있고, 4가지 유형을 모두 선택했다면, 시험은 단어 수 * 유형 수 = 8개의 문제를 출제한다. 단어와 유형은 랜덤으로 선택된다. 예를 들어
  - 문제 1) VocaBokum이 N2에 대해서 유형 4에 대한 문제를 낸다.
  - 문제 2) VocaBokum이 N1에 대해서 유형 1에 대한 문제를 낸다.
  - 문제 3) VocaBokum이 N2에 대해서 유형 1에 대한 문제를 낸다.
  - 문제 4) VocaBokum이 N2에 대해서 유형 2에 대한 문제를 낸다.
  - 문제 5) VocaBokum이 N1에 대해서 유형 4에 대한 문제를 낸다.
  - 문제 6) VocaBokum이 N1에 대해서 유형 3에 대한 문제를 낸다.
  - 문제 7) VocaBokum이 N2에 대해서 유형 3에 대한 문제를 낸다.
  - 문제 8) VocaBokum이 N1에 대해서 유형 2에 대한 문제를 낸다.
- 문제 수를 선택한 경우, 선택한 단어장의 단어들 중에서 가중치 기반 랜덤 선택으로 문제를 생성한다.
  - 100% 랜덤은 아니고, 많이 틀렸던 단어가 다시 출제되는 확률이 더 높다.
  - 설정한 문제 수만큼 가중치 기반으로 단어를 선택하고(중복 가능), 각 문제마다 선택된 유형 중 1개를 랜덤하게 선택해서 출제한다.
  - 최대 문제 수는 단어 수 × 선택된 유형 수이다.
- 유형 1과 3의 경우, 사용자는 한글 뜻을 입력해야 하는데, 단어에 뜻이 여러 개 있을 경우, 1개만 입력해도 정답 처리를 한다.
  - 답변 검증 시 괄호, 쉼표, 마침표 등 특수문자는 무시한다.
  - 공백(white space)도 무시한다. 예: "땅에 묻다"와 "땅에묻다"는 동일하게 처리된다.
  - 한국어 동사의 능동태/피동태/사동태 구분을 하지 않는다. 예: "고정하다", "고정시키다", "고정되다"는 모두 정답으로 인정된다.
- 사용자는 새로 익힐 단어 또는 숙어를 입력할 때 영어 단어/숙어와 한글 뜻을 함께 입력한다. 영어 발음은 인터넷 OpenAPI를 통해서 자동으로 가져온다. 한글 뜻은 여러 개 입력할 수 있으며, 쉼표(,)로 구분한다.
  - 단일 단어 예: "hello" → "안녕, 인사"
  - 숙어 예: "at first" → "처음에는"
  - API에서 발음을 찾지 못하는 경우(주로 숙어) 기본 발음 형식을 사용한다.
- 사용자는 단어장 단위로 학습 대상 단어를 관리할 수 있다.
  - 먼저 단어장을 생성한다 (제목과 선택적 설명 입력).
  - 단어장을 선택한 후 해당 단어장에 단어를 추가한다.
  - 각 단어는 특정 단어장에 속하며, 단어장 정보와 함께 저장된다.
  - 기존 날짜 기반 단어들은 자동으로 "YYYY-MM-DD 단어장" 형태의 단어장으로 마이그레이션된다.
  - 단어장 목록은 최신순(생성일 기준 내림차순)으로 정렬하여 표시된다. 가장 최근에 생성된 단어장이 가장 먼저 표시된다.
- 시험 기능에서는 단어장을 선택(멀티 셀렉트)하여 특정 단어장의 단어들을 대상으로 시험을 볼 수 있다.
  - 한 개의 단어장만 선택하여 시험 보기
  - 여러 단어장을 선택하여 종합 시험 보기
- 사용자가 학습 대상으로 입력한 단어들을 import / export 할 수 있는 기능이 제공된다.
- 관리자 페이지를 제공한다. 관리자 페이지에서는 단어장별 영어 단어들 + 영어 단어 별 시험 합격 비율을 보여준다.
- 관리자 페이지에서 통계 리셋 기능을 제공한다.
  - 전체 리셋: 모든 단어의 통계 데이터(attempts, correct)를 0으로 초기화한다. 단어 자체는 삭제하지 않는다.
  - 단어장별 리셋: 특정 단어장의 모든 단어의 통계 데이터를 0으로 초기화한다.
  - 단어별 리셋: 선택한 특정 단어의 통계 데이터만 0으로 초기화한다.
  - 리셋 작업은 위험한 작업이므로, 실행 전에 확인 다이얼로그를 표시하여 사용자의 명시적 확인을 받는다.
- 플래시 카드 학습 기능을 제공한다.
  - 플래시 카드는 단어를 빠르게 암기했는지 자기 평가하는 학습 방식이다.
  - 시험과 유사한 초기 설정 화면을 제공한다:
    - 단어장 선택: 학습할 단어장을 선택한다 (멀티 셀렉트 가능).
    - 출제할 유형: 3가지 유형 중 학습할 유형을 선택한다. 기본값은 유형 1이다.
    - 채점 방식: 항상 즉시 채점 모드로 고정되며 선택 불가능하다.
    - 문제 수: 선택 불가능하며 선택한 단어장의 모든 단어를 학습한다.
  - 학습 시작 버튼을 누르면 선택한 단어장과 유형에 해당하는 플래시 카드가 무작위로 표시된다.
  - 플래시 카드 학습 흐름:
    1. 문제만 표시 (정답은 숨김 상태):
       - 유형 1: 발음 버튼만 표시 (발음 듣고 영어 단어와 한글 뜻 연상하기)
       - 유형 2: 영어 단어만 표시 (영어 단어 보고 한글 뜻 연상하기)
       - 유형 3: 한글 뜻만 표시 (한글 뜻 보고 영어 단어 연상하기)
    2. 사용자는 문제를 보고 스스로 답을 생각한 후 O 또는 X를 선택:
       - O: 답을 알고 있음 (외웠음)
       - X: 답을 모르거나 확실하지 않음 (아직 못 외웠음)
    3. O 또는 X를 선택하면 정답을 화면에 표시:
       - 유형 1: 영어 단어 + 한글 뜻 모두 표시
       - 유형 2: 한글 뜻 표시
       - 유형 3: 영어 단어 표시
    4. 정답 확인 후 다음 카드로 이동:
       - O를 선택했던 경우: 해당 단어와 유형 조합을 문제 풀에서 제거
       - X를 선택했던 경우: 계속 출제 대상으로 유지
  - 사용자는 답을 직접 입력하지 않으며, 자기 평가(O/X)만 한다.
  - 외웠다고 표시한(O 선택) 단어와 유형 조합은 더 이상 출제되지 않는다.
  - 모든 단어와 유형 조합을 외웠다면(더 이상 출제할 문제가 없으면) 학습 종료:
    - 학습 통계 표시: 총 학습한 문제 수, 학습 시간, 유형별 학습 단어 수 등
    - 축하 메시지 표시
    - 홈으로 돌아가기 또는 다시 학습하기 버튼 제공
  - 참고: 시험 기능은 여전히 4가지 유형(발음 듣고 한글 뜻, 발음 듣고 영어 단어, 영어 단어 보고 한글 뜻, 한글 뜻 보고 영어 단어)을 사용한다. 플래시 카드의 3가지 유형은 플래시 카드 학습 전용이다.

# 아키텍쳐
- B/E 가 없는 F/E 전용 애플리케이션이다.
- 사용자가 입력하거나 import 한 학습 대상 단어는 로컬 스토리지에 저장이 된다.
- react + shadcn 조합을 사용한다.

